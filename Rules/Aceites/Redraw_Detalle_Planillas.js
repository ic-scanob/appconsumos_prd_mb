/**
 * Describe this function...
 * @param {IClientAPI} clientAPI
 */
export default function Redraw_Detalle_Planillas(context) {
    const pageProxy = context.getPageProxy();
    pageProxy.redraw()
}
